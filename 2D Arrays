// Output: 1 

int main() 
{ 
    int nums[3][2] = {   // int nums is a 2D array, an array containing 3 different arrays  
                                      // [3] represents the total contained arrays 
                                      // [2] represents the total amount of items within each contained array 
        {1, 2},   // First array 
        {3, 4},   // Second array 
        {5, 6}   // Third array 
    }; 
    
    printf("%d", nums[0][0]);   /*nums[0][0] represents the item in index position 0 of the array with  
                                an index position of 0 – the number 1 from array {1, 2}*/ 
    
    return 0; 
} 
